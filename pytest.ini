[pytest]
testpaths = tests
markers =
    network: hits live external APIs (ESPN); excluded from the default run
# Keep the default run hermetic and offline. The network suite runs monthly in
# CI, or on demand with:  pytest -m network
addopts = -m "not network"
